{
  "name": "data-insight-sql-dashboard-pro-friendly-editable",
  "version": "2.5.0",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check api/health.js && node --check api/query.js && node --check backend-node/server.js",
    "serve": "python3 -m http.server 8080",
    "vercel-dev": "vercel dev"
  },
  "dependencies": {
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "vercel": "^46.0.0"
  },
  "description": "Data Insight SQL Dashboard Pro Friendly with reference tables, editable records, blue light/dark theme, Business Analytics, SQLite, MySQL, Tableau and Power BI add-on"
}